import { ParentComponent } from 'solid-js';
import { AnyFormOptions, AnyFormState, BroadcastFormSubmissionState } from '@tanstack/form-core';
import { Dayjs } from 'dayjs';
type BroadcastFormSubmissionStateWithoutId = BroadcastFormSubmissionState extends infer T ? T extends any ? Omit<T, 'id'> : never : never;
export type DevtoolsFormState = {
    id: string;
    state: AnyFormState;
    date: Dayjs;
    options: AnyFormOptions;
    history: Array<BroadcastFormSubmissionStateWithoutId>;
};
export declare const FormEventClientProvider: ParentComponent;
export declare function useFormEventClient(): {
    store: import('solid-js').Accessor<DevtoolsFormState[]>;
};
export {};
